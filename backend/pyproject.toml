[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
ignore = [
    # SQLAlchemy filter clauses use `Column == True/False/None` idiomatically;
    # this is not a plain Python truthiness/identity check.
    "E711",
    "E712",
]
